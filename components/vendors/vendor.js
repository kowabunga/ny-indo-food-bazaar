export default function Vendor(vendor) {
  return <div></div>;
}

export default async function getStaticProps() {
  try {
    //@TODO get individual vendor routes
  } catch (error) {
    console.error(error);
  }
}
