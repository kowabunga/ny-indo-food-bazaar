// import {} from '../types';

// eslint-disable-next-line
export default function vendorReducer(state, action) {
  const { type, payload } = action;

  switch (type) {
    default:
      break;
  }
}
