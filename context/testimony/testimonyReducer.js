// import {} from '../types';

// eslint-disable-next-line
export default function testimonyReducer(state, action) {
  const { type, payload } = action;

  switch (type) {
    default:
      break;
  }
}
